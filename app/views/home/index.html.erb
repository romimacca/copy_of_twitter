<h1>Home#index</h1>

<% if user_signed_in? %>
    <%= render 'tweets/form', tweet: @tweet %> 
<% end %>

<div class="row">
    <% @tweets.each do |tweet| %>
        <%= render "shared/tweet_box", tweet: tweet %> 
    <% end %>

    <%= paginate @tweets %>
</div>